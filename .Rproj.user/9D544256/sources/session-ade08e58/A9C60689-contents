# missig values

is.na(airquality)
colSums(is.na(airquality))

# Observation:
# Ozone has 37 missing values.
# Solar.R has 7 missing values.
# Wind, Temp, Month, and Day have no missing values.

# Duplicate rows

?duplicated
duplicated(airquality)

# Observation:
# No duplicate rows were found in the airquality dataset.

# Impossible values

summary(airquality)
# Observation:
# No obviously impossible values were found from the minimum and maximum ranges.

# data type

str(airquality)
?type

typeof(airquality$Ozone)
typeof(airquality$Solar.R)
typeof(airquality$Wind)
typeof(airquality$Temp)
typeof(airquality$Month)
typeof(airquality$Day)


# Outliers

boxplot(airquality$Ozone)
boxplot(airquality$Solar.R)
boxplot(airquality$Wind)
boxplot(airquality$Temp)
boxplot(airquality$Month)
boxplot(airquality$Day)


# Outlier observation:
# Ozone has 2 potential outliers.
# Solar.R has no potential outliers.
# Wind has 3 potential outliers.
# Temp, Month, and Day show no potential outliers.